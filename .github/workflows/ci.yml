name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read
  security-events: write

jobs:
  ci:
    uses: aurea-io/aurea-ci/.github/workflows/ci.yml@main
    with:
      project-type: node-frontend
