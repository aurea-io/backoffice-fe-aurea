name: Release # Contract tracked by aurea-ci#24

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: aurea-io/aurea-ci/.github/workflows/release.yml@v3
    secrets: inherit
