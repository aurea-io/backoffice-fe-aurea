name: Release
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: write
  actions: write
jobs:
  release:
    uses: aurea-io/aurea-ci/.github/workflows/release.yml@v1
    secrets: inherit
