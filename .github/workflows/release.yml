name: Release # Contract tracked by backoffice issue #31

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: aurea-io/aurea-ci/.github/workflows/release.yml@dcda2ac35e98d84fe92aa7461ffc95e885af7161
